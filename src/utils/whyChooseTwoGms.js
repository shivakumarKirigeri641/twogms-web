const whyChooseTwoGms = () => {
  let array = [];
  array.push("✅ Easy to use for both customers and garages");
  array.push("✅ Cloud-based and mobile-friendly platform");
  array.push("✅ Secure login for each garage (multi-tenant system)");
  array.push("Custom features for every garage type");
  return array;
};
export default whyChooseTwoGms;
